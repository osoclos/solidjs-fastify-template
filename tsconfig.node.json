{
    "extends": ["./tsconfig.json"],
    "include": ["index.ts", "src/server", "src/shared", "vite.config.ts"],

    "compilerOptions": {
        // Modules
        "paths": {"~/*": ["./src/server/*"] },
        "types": ["@types/bun"],

        // Language and Environment
        "lib": ["ESNext"],
        "target": "ESNext",
    }
}
